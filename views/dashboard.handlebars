<header><h1>Your Dashboard</h1></header> 
{{!-- use for dashboard --}}

 {{!-- use partial here with the length thing --}}

{{#if posts.length}}


    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="btn btn-primary">edit post</button></a>
            </div>
    {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">you do not currently have any posts</h6>
{{/if}}

{{!-- button for adding stuff in  --}}
 <div id="manipulate"> <a href="/dashboard/create"><button type="button" class="btn btn-primary" id="maniputlate">+ New Post</button></a></div>